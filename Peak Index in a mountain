class Solution {
    public int peakIndexInMountainArray(int[] arr) {
        int c=0;
        for(int i=0;i<arr.length;i++)
        {
            if(arr[i]<arr[i+1])
                continue;
            else
            {
                c=i;break;
            }
        }
        return c;
    }
}
